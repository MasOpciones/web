---
interface Props {
  source?: string;
  note?: string;
}

const { source, note } = Astro.props;
---

<footer class="act-footer">
  {source && <p><strong>Fuente:</strong> {source}</p>}
  {note && <p>{note}</p>}
</footer>